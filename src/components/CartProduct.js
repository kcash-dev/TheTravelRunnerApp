import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const CartProduct = ({ item }) => {
    return (
        <View>
            <Text>This is a cart product</Text>
        </View>
    )
}

export default CartProduct

const styles = StyleSheet.create({})
